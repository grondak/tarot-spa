---
scope: tarot-spa multiuser accounts + LLM Orientation Guide release
purpose: build-substrate
altitude: feature
updated: 2026-07-11T11:08
---

- (constraint) [ADOPTED] Brownfield frontend: React 19.2 + Vite 7.3 + Tailwind CSS v4 (CSS-first, no tailwind.config.js), plain JS/JSX (no TypeScript at runtime), ESLint 9 flat config. Source: project-context.md. Frontend conventions carry forward unchanged; do not introduce TS/PropTypes at runtime without an explicit decision.
- (constraint) [ADOPTED] Hardcoded base path '/tarot-spa/' in vite.config.js for GitHub Pages — any new routing/asset/URL handling must account for this. Source: project-context.md.
- (constraint) [ADOPTED] AWS-native direction confirmed by Tony: Cognito (auth), Bedrock (LLM), API Gateway + Lambda (API), heavy use of Amplify for hosting/CDN/auth-glue ('meant to be used'). Amplify hosting replaces GH Pages, not supplements it. Source: addendum.md.
- (constraint) [ADOPTED] Interaction model is one-shot per Session — Draw + Current Events + Lens + Guide generated in one user-facing request, no multi-turn conversation state to persist server-side. Source: prd.md FR-8, addendum.md.
- (constraint) [ADOPTED] Cost guardrails already fixed by PRD: per-Account Daily Orientation Limit (FR-9, configurable, server-enforced) + aggregate /month budget ceiling with alerting (FR-10). Both need real enforcement mechanisms in this architecture. Source: prd.md.
- (constraint) [ADOPTED] Two-generation Invite Key system (FR-1/2/3), Public Landing + Request Access form (FR-4/5, no persistence — transactional email only), Admin Dashboard (FR-11/12, Tony-only, deliberately low-design). Survey/Follow-Up Nudge cut from v1 entirely. Source: prd.md.
- (constraint) [ADOPTED] UX spine (EXPERIENCE.md) fixes: single responsive web surface (no separate mobile IA); new 'Quick Draw' mode with zero LLM/DB-write involvement, free/unlimited, reachable both publicly (unauthenticated) and authenticated; Orientation Guide Results is a transient view-state, not a persisted/bookmarkable route; rate-limit-exhausted state degrades gracefully to Quick Draw rather than hard-rejecting (supersedes prd.md FR-9 wording — flagged there as a pending PRD edit). Source: EXPERIENCE.md.
- (constraint) [ADOPTED] Existing draw-code encode/decode (client-only, no server) is the pre-existing anonymous sharing mechanism — project-context.md flags an open call on whether it stays as-is or gets superseded by real persistence once accounts exist. Carried into this run as a genuine open question, not pre-resolved.
- (version) Web-verified (2026-07-10): AWS Amplify Gen 2 is the current paved path — code-first TypeScript backend definitions (amplify/ folder) provisioning AppSync GraphQL + DynamoDB (Data), Cognito (Auth), and Lambda (Functions) together. Amplify's own backend-definition TS is separate from the frontend's plain-JS convention, no conflict. Source: docs.amplify.aws.
- (version) Web-verified (2026-07-10): Current Bedrock model lineup includes Claude (Opus/Sonnet/Haiku 4.x family) and Amazon Nova 2 (Lite/Sonic/Multimodal). Claude per-million-token pricing roughly Opus ~$5/$25, Sonnet ~$3/$15, Haiku ~$1/$5 (in/out). Batch inference is ~50% cheaper; prompt caching up to ~90% off cached input. Source: aws.amazon.com/bedrock/pricing, pricing aggregators.
- (version) Web-verified (2026-07-10): Two live grounding options on Bedrock — (1) Amazon Nova Web Grounding: a turnkey built-in systemTool for Nova models only, model decides when to search, returns cited results, RAG-in-a-box. (2) Bedrock AgentCore Web Search: a separate managed MCP-based tool usable via AgentCore Gateway with any model (incl. Claude), more general but adds an AgentCore agent-runtime layer. No equivalent single-call 'just give Claude search' primitive exists directly on the plain Converse API today. Source: aws.amazon.com/blogs/aws, aws.amazon.com/blogs/machine-learning.
- (decision) Backend starter/paradigm: AWS Amplify Gen 2 (code-first TypeScript backend definition in amplify/, provisioning Cognito Auth + AppSync GraphQL API + DynamoDB Data + Lambda Functions together). Confirmed by Tony over a hand-rolled API Gateway REST + separate DB approach. Binds all backend work; prevents ad-hoc wiring of Cognito/API/DB as separate hand-integrated services.
- (decision) LLM + Current Events grounding: Claude (model tier TBD next) via Bedrock, with a plain external search API called directly from a Lambda function BEFORE the generation call — not Bedrock AgentCore, not Nova Web Grounding. Confirmed by Tony. Prevents adopting a whole separate AgentCore agent-runtime service for a hobby app, and avoids Nova's untested prose-quality risk for the essay task.
- (version) Web-verified (2026-07-10): Tavily search API fits the external-search role well for FR-8 grounding — 1,000 free credits/month (no card required), 1 credit per Basic Search, pay-as-you-go $0.008/credit beyond that. Purpose-built for LLM/agent consumption (returns pre-extracted content, single-step call). Source: tavily.com/pricing, docs.tavily.com.
- (decision) LLM model tier: Claude Opus (via Bedrock), chosen over Sonnet/Haiku after real per-request cost comparison (~$0.03/request, ~1,000 requests/month headroom under the $30 FR-10 ceiling). Tony explicitly prioritized best plausible essay quality over growth headroom at this stage, given FR-8's core risk is the 'abstract miss' quality failure (UJ-2), not cost. Revisit if usage approaches the ~1,000/month ceiling.
- (decision) Search API: Tavily, confirmed by Tony. Called directly from the Orientation Guide Lambda before the Claude generation call (plain HTTP call, not a Bedrock-native tool).
- (decision) Rate limit + budget enforcement: app-level atomic counter (DynamoDB conditional UpdateItem, increment-and-check in one atomic operation) for both the per-Account Daily Orientation Limit (FR-9) and the aggregate monthly spend (FR-10), confirmed by Tony. AWS Budgets + SNS alert stays as a secondary safety-net alert only (billing-data lag makes it unsuitable as the real-time blocking mechanism).
- (assumption) Tony's real expected usage: ~20 total users, ~0.5 sessions/day/user on average, 1-2 Orientation Guide requests per active session-day. Expected volume ~450-600 requests/month, worst-realistic-case well under 1,200/month. At Claude Opus (~$0.03/request) this is ~$13-18/month even at the higher end — comfortably under the $30 FR-10 ceiling with real margin. Confirms Opus is safe at actual expected scale; the ceiling is a guard against bugs/abuse, not a binding constraint on normal usage.
- (decision) Deployment & environments: add a 'staging' git branch as a fully isolated Amplify environment (own Cognito pool, own DynamoDB tables, own Lambda, password-protected URL) alongside 'main' (prod). Workflow: push/test on staging, merge to main to promote. Confirmed by Tony, given this release introduces real auth/data/LLM-spend for the first time versus today's static-only GH Pages deploy.
- (decision) Data model confirmed by Tony: Account, InviteKey, Session, DailyUsage (accountId#date key), MonthlySpend (year-month key) as Amplify Data models. Design paradigm confirmed: thin Lambda-function-per-capability, no repository/DI abstraction layers, matching the existing frontend's minimal-abstraction style.
- (decision) Daily Orientation Limit 'calendar day' = UTC, confirmed by Tony.
- (direction) Deferred (Tony, explicit 'not for now'): capturing per-user timezone and displaying limit-reset/timestamps in the user's local time is a real future improvement, not v1. UTC-everywhere is fine until then.
- (decision) Admin Dashboard (FR-11) stays strictly aggregate/metrics-only, no built-in raw-content (Session Context/Guide) viewer feature. Confirmed by Tony. Resolves prd.md's open Privacy question: operator access to raw user data is deliberately NOT a built-in app capability — closes the adversarial review's flagged gap (operator access was previously unscoped).
- (decision) Draw-code sharing mechanism (existing client-only encode/decode) resolved: stays exactly as-is, used only by Quick Draw (public + authenticated variants), which has zero DB/LLM involvement. No conflict with persisted Sessions (Orientation Guide flow) — different features, different persistence needs. Closes project-context.md's open call.
- (decision) [ADOPTED] Authorization mechanism: Amplify Data's built-in owner-based authorization rule on Account/Session/DailyUsage records (each item readable/writable only by its owning Cognito identity) enforces the Cross-Cutting Security NFR ('no Account can access another Account's data'). Admin-only access (Admin Dashboard aggregate queries, key minting) gated via a Cognito group/custom claim, not per-record ownership. Adopted as the idiomatic Amplify Gen 2 mechanism for the already-confirmed starter, not re-litigated as a separate fork.
- (decision) Daily Orientation Limit / monthly budget ceiling adjustment mechanism (prd.md Open Question 3, previously unresolved): a single Config item in DynamoDB (dailyLimit, monthlyBudget fields) read by the enforcement Lambdas, editable via a plain field on the Admin Dashboard. Confirmed by Tony. No new infrastructure — reuses the already-planned Admin Dashboard.
- (decision) Tavily slow-but-not-failed behavior (prd.md FR-8 NFR/Open Question 5, previously punted to architecture): 20-second timeout (not 5s, per Tony's explicit correction), then proceed to the Claude Opus generation call without Current-Events grounding rather than hang indefinitely. On timeout, user-facing copy notes it playfully — Tony's own framing: 'the news is slow today, ha ha' — rather than a dry error message.
- (assumption) 20s Tavily timeout means worst-case total generation latency (search timeout + Claude Opus generation time) can exceed FR-8's previously-logged ~20s end-to-end latency target — that target was already tagged [ASSUMPTION]/unvalidated pending a real model benchmark, so no conflict requiring resolution now, but flagged for revisit once real latency is measured.
- (decision) Request-Access form (FR-5) email delivery: Amazon SES, confirmed by Tony, accepting the one-time sandbox recipient-verification step. Stays fully AWS-native rather than adding a third-party form-service dependency for one small feature.
- (event) Patched ARCHITECTURE-SPINE.md with AD-13 (Config item for limit/budget, DynamoDB + Admin Dashboard field), AD-14 (20s Tavily timeout, proceed ungrounded, playful copy), AD-15 (SES for FR-5 email) — closing all 3 gaps the distillation subagent flagged. Updated Stack, source tree, structural diagram, and Consistency Conventions accordingly; removed the 3 resolved items from Deferred. Moving to Reviewer Gate.
- (event) Reviewer Gate ran: rubric walker (1 critical, 1 high, 2 medium, 2 low), adversarial incompatibility hunt (3 critical, 4 high, 2 medium, 1 low), tech-verification (0 critical/high, 1 medium, 4 low). Reviews written to reviews/review-rubric.md, review-adversarial.md, review-tech-verification.md. Applying fixes.
- (decision) AD-2/AD-11 base-path conflict resolved: drop the GH-Pages-era '/tarot-spa/' path entirely, migrate to a clean root '/' on Amplify Hosting. Confirmed by Tony — old bookmarked/shared links under the old path break, accepted given the scope of this release already.
- (decision) InviteKey redemption (FR-1) atomicity, closing rubric-critical + adversarial-high findings: redemption is a single atomic conditional UpdateItem on InviteKey (status unredeemed->redeemed), performed inside a Cognito post-confirmation Lambda trigger — the idiomatic Amplify Gen2 mechanism. If the conditional write fails (already redeemed/revoked by a concurrent request), the trigger rejects the signup so Cognito and InviteKey state never diverge. Same-identity multi-key redemption prevented via verified-email uniqueness check across Accounts at redemption time.
- (decision) AD-6 rewritten to close the pre-flight/post-flight ambiguity (adversarial-critical): two-phase protocol — (1) atomic pre-flight reservation: check-and-increment DailyUsage by 1 and MonthlySpend by a fixed per-request cost estimate (~/bin/zsh.03, per the Opus math already in Deferred) in one conditional UpdateItem each, rejecting the request up front if either would exceed its Config ceiling; (2) compensating rollback: only an outright Tavily/Bedrock failure (not a Tavily timeout, which still produces a delivered ungrounded Guide per AD-14) triggers an atomic decrement of both reservations. MonthlySpend is intentionally an estimate-based real-time gate, not a precise post-call ledger; AWS Budgets remains the safety net that catches estimate-vs-actual drift over time.
- (decision) Tavily-timeout-then-ungrounded-Guide (AD-14) explicitly counts as a confirmed successful completion for DailyUsage/MonthlySpend purposes (closes adversarial-high finding: otherwise a free/uncounted-request path via repeated timeouts). Only an outright Tavily exception or outright Bedrock/Claude failure qualifies for the AD-6 rollback carve-out.
- (decision) MonthlySpend authorization (closes adversarial-critical: was missing from AD-9's Binds): no owner-based rule (it has no owning identity, it's a cross-account aggregate). Read access is admin-group-gated via Amplify Data auth; write access restricted to the usage-counter Lambda's IAM execution role only, never a client-writable field or public mutation.
- (decision) Onward-key-mint eligibility (closes adversarial-critical: no check previously required generation==First-Gen), new AD: Account gets an onwardKeyGenerated boolean field (extends AD-8). The invite-key-mint Lambda's onward-key path is a single atomic conditional UpdateItem: generation=FirstGen AND onwardKeyGenerated=false -> true, in the same transaction as creating the new InviteKey. Tony's own direct admin-mint path (FR-3) is a separate, unrestricted admin-only code path.
- (decision) Lambda-to-DynamoDB write path (closes adversarial-medium): server-side Lambda writes use Amplify Gen2's IAM-authorized function-scoped data client (per-function resource grants, e.g. usage-counter gets write access to DailyUsage/MonthlySpend/Config only), not the client-facing owner-based GraphQL mutations. AD-9's owner-based rule constrains client-originated (browser->AppSync) mutations only.
- (decision) FR-11 Admin Dashboard aggregate-metrics computation mechanism (closes rubric-medium, previously unstated): a 5th Lambda, admin-metrics, following the same thin-Lambda-per-capability paradigm as every other capability (AD-4) rather than client-side raw list-query aggregation.
- (version) Web-verified (2026-07-11, via reviewer-gate tech-verification pass): current-generation Claude Opus on Bedrock requires an inference-profile identifier (not a bare foundation-model ID) for most versions/regions -- Opus 4.5/4.6 always need one, Opus 4.7/4.8 need a cross-region prefix outside select regions. Flagged as an implementation-time detail on AD-5. Also independently reconfirmed: SES sandbox behavior (verify-once, 200/24hr + 1/sec quota), Amplify Hosting per-branch password protection (BasicAuthConfig) -- both already stated correctly in the spine, no content change needed.
- (version) Web-verified (2026-07-11, via reviewer-gate tech-verification pass, backfilling two claims made without citation during the original coaching session): Amazon SES sandbox mode requires verifying both sender and recipient addresses, 200 emails/24hr + 1/sec quota, lifted on production access request (docs.aws.amazon.com/ses) — confirms AD-15 as stated. Amplify Hosting's BasicAuthConfig supports per-branch (not just per-app) password protection via Console > Hosting > Access control or CloudFormation (docs.aws.amazon.com/amplify) — confirms AD-11's staging design is achievable as described.
- (event) All Reviewer Gate findings resolved: AD-2/AD-6/AD-9/AD-11/AD-13/AD-14/AD-4/Design-Paradigm edited in place; AD-16 (InviteKey redemption), AD-17 (onward-key eligibility), AD-18 (admin-metrics Lambda) added. Fixed one internal inconsistency found during final read-through (AD-17 referenced a nonexistent admin-metrics minting path; corrected to invite-key-mint, admin-gated). lint_spine.py clean (0 findings) after all edits. spine finalized
